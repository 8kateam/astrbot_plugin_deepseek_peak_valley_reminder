## 0.2.2

`/ds` 查询指令的回复内容支持自定义。

- 新增 `ds_peak_message`、`ds_valley_message`、`ds_weekend_message` 三个配置项，分别对应查询结果为峰时、工作日谷时、周末谷时的回复，可配置多条并随机抽取。
- 自定义文案同样支持 `{time}` 占位符（替换为当前时间）与 `\n` 换行。
- 配置留空时自动回退为内置默认文案。

## 0.2.1

修复工作日切换点提醒偶发漏发的问题。

- 为定时任务增加 1 小时的 misfire 宽限期：整点时事件循环短暂繁忙不再导致提醒被直接跳过。
- 新增启动补偿：插件启动/重载时，若发现当天某个切换点在最近 30 分钟内已过且尚未发送（例如 Bot 开机或重载插件晚于整点），自动补发该条提醒。
- 通过 KV 存储记录当天各切换点的发送状态，保证正常触发与补偿之间不会重复推送。

## 0.2.0

适配 DeepSeek 新计价规则：周六与周日（UTC+8）全天按谷时计价。

- 周末全天视为谷时，`/ds` 查询在周末显示专属提示文案。
- 峰谷切换定时提醒仅在周一至周五触发，避免周末误发「已进入峰时」的误导消息。
- 新增 `enable_weekend_valley` 配置项（默认开启），可一键关闭该行为、恢复旧的按小时判定逻辑。

## 0.1.1

现在在 WebUI 文案配置中写 第一行\n第二行 即可实现换行。

## 0.1.0

首个可用版本，实现定时推送、查询当前峰谷时、自选推送群聊/私聊、自定义推送时的消息等功能。