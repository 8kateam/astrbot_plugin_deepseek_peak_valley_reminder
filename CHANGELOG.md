## 0.2.0

适配 DeepSeek 新计价规则：周六与周日（UTC+8）全天按谷时计价。

- 周末全天视为谷时，`/ds` 查询在周末显示专属提示文案。
- 峰谷切换定时提醒仅在周一至周五触发，避免周末误发「已进入峰时」的误导消息。
- 新增 `enable_weekend_valley` 配置项（默认开启），可一键关闭该行为、恢复旧的按小时判定逻辑。

## 0.1.1

现在在 WebUI 文案配置中写 第一行\n第二行 即可实现换行。

## 0.1.0

首个可用版本，实现定时推送、查询当前峰谷时、自选推送群聊/私聊、自定义推送时的消息等功能。